import 'package:fluent_ui/fluent_ui.dart';

class PrefileErrorPage extends StatelessWidget {
  const PrefileErrorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
